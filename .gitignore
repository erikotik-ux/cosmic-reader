# Dependencies
node_modules/

# npm logs
npm-debug.log*
.npm

# OS files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/

# Tailwind source (keep config + input.css, skip generated output if rebuilding from source)
# cosmic-reader.css is intentionally committed so the site works without a build step

.vercel

# Local-only project notes / dev artifacts (don't ship to Vercel)
.claude/
CLAUDE.md
NOTEBOOKLM_SUMMARY.md
COSMIC_READER_PROJECT_NOTES.md
pinecone-pending-upserts.md
pinecone-setup.py

# Font / page backups — kept locally for rollback, not deployed
.font-backup-space-grotesk/
app.backup.html
galaxy-map.backup.html

# Local-only FX/demo previews — not deployed
planet-fx-demo.html

# Graphify knowledge-graph output — regenerated by the hook on every run,
# so tracking it produces constant commit churn. Excluded from deploys too
# (see .vercelignore).
graphify-out/
